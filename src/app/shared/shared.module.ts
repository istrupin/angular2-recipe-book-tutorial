import { NgModule } from "@angular/core";
import { CommonModule } from "@angular/common";

@NgModule({
    imports: [CommonModule], //don't actually need this import since we are not using it
    exports: [CommonModule]
})
export class SharedModule{ }